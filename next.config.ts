import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",
  // // reactStrictMode: true,
  // trailingSlash: true,
  // // 필요시 basePath, i18n, rewrites 등 추가 가능
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
